The book provides the base application structure. I completed the project by implementing the following:

1. Added log level output by modifying the logf and the corresponding logging functions (Debugf, Infof, etc.)

2. Level convertion (from byte to string type using Stringer interface)

3. Log truncation at a given amount of characters